[package]
name = "wasm-rust"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
linktime = { path = "../../../linktime" }
libc-print = "0.3"

[workspace]

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = [
    'cfg(wasmtime)',
] }

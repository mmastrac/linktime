[package]
name = "warn-unsafe"
edition = "2021"
publish = false

[dependencies]
ctor = { path = "../../../ctor", default-features = false, features = ["proc_macro", "std"] }
libc-print = "0.3"

[features]

[workspace]

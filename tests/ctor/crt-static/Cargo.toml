[package]
name = "tests-edition-2018"
version = "0.1.0"
edition = "2018"
publish = false

[features]

[dependencies]
ctor = { path = "../../../ctor" }

[workspace]

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(linktime_used_linker)'] }

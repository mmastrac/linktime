[package]
name = "tests"
version = "0.1.0"
edition = "2021"
publish = false

[dev-dependencies]
crok-lib = { version = "0.8.1", default-features = false, features = ["fancy-regex"] }
libc-print = "0.5"

[lib]
path = "test.rs"
